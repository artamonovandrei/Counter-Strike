# path: backend/requirements.txt
# Pinned. Bump deliberately, not accidentally — a python-socketio major bump changes the
# wire format and every connected client has to be rebuilt at the same time.

fastapi==0.141.1
uvicorn[standard]==0.52.0
python-socketio==5.16.3
python-engineio==4.13.4
pydantic==2.13.4
pydantic-settings==2.14.2
python-dotenv==1.2.2

# Optional: only imported when REDIS_URL is set (multi-process fan-out).
redis==8.1.0

# ─── dev / test ───────────────────────────────────────────────────────────────
pytest==9.1.1
httpx==0.28.1
